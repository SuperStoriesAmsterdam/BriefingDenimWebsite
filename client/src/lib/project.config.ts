/**
 * PROJECT CONFIG — update these values when starting a new project.
 *
 * PROJECT_ID      — short slug, used as localStorage key prefix (e.g. "dc", "bm", "vx")
 * PROJECT_NAME    — displayed in the sidebar header
 * PROJECT_VERSION — bump this string whenever prd-defaults.ts changes significantly,
 *                   so the merge logic runs and new defaults reach existing users.
 */
export const PROJECT_ID = "dc";
export const PROJECT_NAME = "Denim City";
export const PROJECT_VERSION = "v36";
